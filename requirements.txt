# Core dependencies
langgraph>=0.1.0
langchain>=0.1.0
pydantic>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# LLM Clients
# - Ollama: local (included via requests)
# - OpenRouter: API-based, fast (included via requests)

# Python version
python>=3.10
dotenv>=0.19.0

# Optional: for testing
pytest>=7.0.0
pytest-cov>=4.0.0
